<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full bg-zinc-900 text-white p-5">
        
        <div class="w-full flex justify-between">
            <div>
                <div class="flex gap-2 items-center">
                    <div class="w-8 h-8 rounded-full object-cover overflow-hidden">
                        <img src="/images/uploads/<%=user.profilePic%>"/>
                    </div>
                    <h1 class="text-3xl font-semibold">Hello, <%=user.userName%> 👋</h1>
                </div>
                <p class="mb-14 text-zinc-500 text-sm">You cam create a new post</p>
            </div>
            <a href="logout" class="px-5 py-2 h-full rounded-md bg-red-500">Logout</a>
        </div>
        <form method="post" action="/post">
            <textarea class="py-2 w-1/3 px-2 resize-none rounded-md bg-transparent border-2 border-zinc-800 outline-none" name="content" placeholder="Whats in your mind"></textarea><br/>
            <input class="py-2 px-5 rounded-md bg-yellow-500 text-black ouline-none" type="submit" value="Create Post"/>
        </form>

        <p class="mt-14 mb-5 text-zinc-500">Your posts</p>
        <div class="flex gap-2 flex-col flex-wrap">
            <%user.posts.reverse().map((post, index)=>{%>
                <div class="w-1/3 bg-zinc-800 rounded-lg p-4">
                    <h3 class="text-blue-500 mb-1"><%=user.email%></h3>
                    <p class=""><%=post.content%></p>
                    <div class="mt-3 flex gap-3">
                        <a href="/like/<%=post._id%>" class="text-sm"><span><%=post.likes.length%></span><%=post.likes.indexOf(user._id) == -1 ? "🤍" : "❤️"%></a>
                        <a href="/update/<%=post._id%>" class="text-zinc-500 text-sm">Edit</a>
                        <a href="/delete/<%=post._id%>" class="text-red-500 text-sm">Delete</a>
                    </div>
                </div> 
            <%})%>
            
        </div>
    </div>
</body>
</html>